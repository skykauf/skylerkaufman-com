# Agent skills (canonical)

Markdown skills for this repo live here as the **single source of truth**. The same skill directories are linked for tool discovery:

- **Cursor:** `.cursor/skills/<name>` → `../../skills/<name>`
- **Claude Code:** `.claude/skills/<name>` → `../../skills/<name>`

Edit files under `skills/` only; symlinks avoid duplicating content. Project-wide pointers also live in [`AGENTS.md`](../AGENTS.md) and [`CLAUDE.md`](../CLAUDE.md).
