# FIVB Leaderboard – ETL and app connection to Postgres
# Copy to .env and set values. Use the same user/password in ~/.dbt/profiles.yml.
#
# Create DB and user first:  DB_PASSWORD=yourpassword ./postgres/setup.sh

# Connection URL (required for ETL and Streamlit)
# Format: postgresql+psycopg2://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql+psycopg2://fivb_leaderboard:yourpassword@localhost:5432/fivb_leaderboard

# Optional: used by postgres/setup.sh and by dbt (when using .dbt/profiles.yml)
# Uncomment and set so dbt can connect; DB_PASSWORD is required for dbt if using project profile.
# DB_NAME=fivb_leaderboard
# DB_USER=fivb_leaderboard
# DB_PASSWORD=yourpassword
# PGHOST=localhost
# PGPORT=5432
#
# Run dbt with project profile: DBT_PROFILES_DIR=.dbt dbt run
